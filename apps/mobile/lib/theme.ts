/** Product palette for Expo. Prefer these over hardcoded Cadre hexes.
 * System chrome (nav bars, fills, labels) stays in `./native` via PlatformColor. */
export { botColors, tokens } from "@cadre/ui-tokens";
